# Kibana baseline placeholder config for Titan-Takeaway training.
# Used when `search` profile is enabled in docker-compose.
server.host: 0.0.0.0
elasticsearch.hosts: ["http://elasticsearch:9200"]
monitoring.ui.container.elasticsearch.enabled: true
